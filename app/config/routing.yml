test:
    pattern: /test
    defaults: { _controller: AppBundle:Default:test }

login:
    pattern: /login
    defaults: { _controller: AppBundle:Security:login }
 
login_check:
    pattern: /login_check
 
logout:
    pattern: /logout

dashboard:
    pattern: /
    defaults: { _controller: AppBundle:Dashboard:index}


article:
    resource: "@AppBundle/Resources/routing/article.yml"
    prefix: /article